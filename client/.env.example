VITE_API_MODE=mock
VITE_API_BASE_URL=http://localhost:4000/api/v1
# Demo-only: shifts the mock 18:00 WAT lock (mirrors the server's CUTOFF_HOUR
# env) so the full submission flow can be demoed after hours. Live mode always
# uses the real server's cutoff.
# VITE_CUTOFF_HOUR=18
